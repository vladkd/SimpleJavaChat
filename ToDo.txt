- Fix Client's send button action listener, so it executes on the UI thread to allow automatic scrolling
- Server needs to be able to run without GUI or without CLI.
- Wrapper for mac osx?
- Android version
